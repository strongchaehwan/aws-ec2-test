name: Github Actions 실행시켜보기

on:
  push:
    branches:
      - main

jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest

    steps:
      - name: Hello wolrd 찍기
        run: echo "Hello world"

      - name: 여러 명렁어 문장 작성하기
        run: |
          echo: "Good"
          echo: "morning"